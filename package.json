{
  "name": "call-insight-service",
  "version": "1.0.0",
  "description": "Backend service that summarizes call/chat transcripts using an LLM and enables retrieval-based Q&A over past conversations",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.60.0",
    "pg": "^8.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
